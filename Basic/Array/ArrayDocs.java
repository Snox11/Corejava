package Basic.Array;

public class ArrayDocs {
    /*
     * Array
     * #used to store and processing static multiple data
     * #collection of homogeneous data
     * #collection or group of multiple values of same data type with fixed size
     * #data is stored on the basis of indices(index starts form 0)
     * #could also contains duplicate data
     * #contiguous memory allocation
     * #In java array is treated as an object
     * 
     * #Types :
     * a> Single dimension array(Eg int[])
     * syntax :
     * data_type array_name[] = new data_type[size];
     * b > Multi Dimensional array (eg int [][])
     * Syntax :
     * data_type array_name[row][column] =new data_type[row][column];
     */

}
